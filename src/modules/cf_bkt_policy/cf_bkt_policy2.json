{
    "Statement": [
      {
        "Effect": "Allow",
        "Action": ["s3:GetObject"],
        "Resource": ["${var.failover_bucket_arn}/*"],
        "Principal": {
          "Service": "cloudfront.amazonaws.com"
        },
        "Condition": {
          "StringEquals": {
            "AWS:SourceArn": [ "${output.cloudfront_arn}" ]
          }
        }
      }
    ]
}

